// Write a function called “computeSquareRoot”.
// Given a number, “computeSquareRoot” returns its square root.

computeSquareRoot = (num) => Math.sqrt(num)

const output = computeSquareRoot(36)
console.log(output)